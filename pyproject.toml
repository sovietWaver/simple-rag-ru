[project]
name = "myrag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers",
    "chromadb",
    "ollama",
    "pypdf",
    "langchain-text-splitters",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["."]
